# 第二课演示——四角度 Review 后迭代结果

**日期**：2026-06-03
**基于**：4 个视角（课程设计/学员/AI 技术/C 嵌入式）的 Review 报告

---

## 迭代总览

| 优先级 | 问题 | 状态 | 修改内容 |
|--------|------|------|---------|
| P0 🔴 | CLAUDE.md 加载位置描述矛盾 | ✅ 已修复 | 演示二改为"稳定段命中缓存"，不硬绑定具体位置，加讲师注意提示 |
| P0 🔴 | Hook 脚本路径不一致 | ✅ 已修复 | 演示四设计文档全部统一为 `hooks/stop_check_syntax.py` |
| P0 🔴 | 缺少演示状态依赖矩阵 | ✅ 已新增 | 新文件 `演示状态依赖矩阵.md`，含依赖关系图 + 独立运行方案 |
| P1 🟡 | security-reviewer Bash ≠ 物理只读 | ✅ 已修复 | 3 个自定义 Agent 全部去掉 Bash，仅保留 Read/Grep/Glob |
| P1 🟡 | CLAUDE.md 缺 MISRA/Coverity | ✅ 已修复 | 新增"代码质量"段：Coverity Scan + MISRA C:2012 + .clang-format |
| P1 🟡 | 演示五现场超时风险 | ✅ 已修复 | 默认方案改为展示预跑报告，时长缩至 8-12min |
| P1 🟡 | Stop Hook 缺 compile_commands.json 方案 | ✅ 已修复 | Step 4 补充工程进阶方向 + clang-tidy 说明 |
| P2 🟢 | 演示四 Python 对 C 工程师认知负担 | ✅ 已修复 | Step 1 开场补充"可用任何语言写" |

---

## 详细修改记录

### 1. 演示二：CLAUDE.md 加载位置（P0）

**修改文件**：`演示二_Langfuse可观测性_执行轨迹.md`

**Before**：
> "CLAUDE.md 出现在 User Message 中（而非 System Prompt）"

**After**：
> "CLAUDE.md 被拼装在 prompt 的稳定段中，确保前缀不变以命中 prompt cache"
> + 讲师注意：具体位置以课前 Langfuse 截图为准

**原因**：AI 技术专家指出 Claude Code 实际实现中 CLAUDE.md 可能在 system prompt 静态段，与"User Content 路径"说法可能不一致。改为技术上更安全的表述。

### 2. 演示四：Hook 路径统一（P0）

**修改文件**：`演示四_StopHook_语法检查门禁.md`

所有 `.claude/hooks/stop_check_syntax.py` → `hooks/stop_check_syntax.py`，与实际文件位置和 settings.local.json 配置一致。

### 3. 新增：演示状态依赖矩阵（P0）

**新文件**：`演示状态依赖矩阵.md`

包含：
- 演示间依赖关系图
- 每个演示的独立运行条件和前序失败应对方案
- 课堂演示顺序建议
- 关键物料检查清单

### 4. 自定义 Agent 去掉 Bash（P1）

**修改文件**：`.claude/agents/` 下 3 个 .md 文件

`tools: Read, Grep, Glob, Bash` → `tools: Read, Grep, Glob`

现在"物理上只能看、不能改"的说法严格成立。

### 5. CLAUDE.md 补充代码质量段（P1）

**修改文件**：`srsRAN_4G/CLAUDE.md`

新增：
```markdown
## 代码质量
- 静态分析：Coverity Scan（CI 集成），本地可用 cppcheck / clang-tidy
- 编码规范参考：MISRA C:2012（Advisory 级别建议遵守）
- 格式化：项目根目录 .clang-format（Google 风格变体）
```

### 6. 演示五默认用预跑报告（P1）

**修改文件**：`演示五_MultiAgent_调研Review迭代.md`

时长从"8-15min"缩至"8-12min"，新增"默认方案：展示预跑报告"。

### 7. Stop Hook 补充 compile_commands.json（P1）

**修改文件**：`演示四_StopHook_语法检查门禁.md`

Step 4 扩展为"防死循环 + 工程进阶方案"，补充 `compile_commands.json` + `clang-tidy` 说明。

### 8. 演示四补充语言说明（P2）

**修改文件**：`演示四_StopHook_语法检查门禁.md`

Step 1 讲述补充："这个脚本用 Python 写只是方便演示，你们完全可以用 C、Shell 或任何语言写。"

---

## 未处理项（需讲师在正文中修改）

| 项目 | 原因 |
|------|------|
| 正文 §6.5 stderr/stdout 伪代码描述 | 正文不在本次迭代范围，已在演示四中加讲师注意提示 |
| Memory/Rules 微演示 | 需在演示二执行时口头补充，不改文件 |
| 课前 Langfuse trace 截图 | 需课前实际操作，非文件迭代 |
| `stop_hook_active` 字段验证 | 需课前用 tee 记录实际 stdin 确认 |

---

## 当前文件清单（迭代后）

```
code/ziguangzhanrui_2/
├── srsRAN_4G/
│   ├── CLAUDE.md                           ← 已更新（+代码质量段）
│   ├── CLAUDE.local.md
│   ├── .claude/
│   │   ├── settings.local.json
│   │   └── agents/
│   │       ├── security-reviewer.md        ← 已更新（去掉 Bash）
│   │       ├── code-quality-reviewer.md    ← 已更新（去掉 Bash）
│   │       └── architecture-reviewer.md    ← 已更新（去掉 Bash）
│   ├── hooks/stop_check_syntax.py
│   └── build/
│
└── 演示设计/
    ├── 演示二_...md                        ← 已更新（加载位置修正）
    ├── 演示四_...md                        ← 已更新（路径统一+进阶方案+语言说明）
    ├── 演示五_...md                        ← 已更新（默认预跑报告+时长+去Bash）
    ├── 演示状态依赖矩阵.md                ← 新增
    ├── 四角度Review汇总报告.md
    ├── 迭代结果报告.md                     ← 本文件
    └── [其他文件不变]
```
