# 用户级 CLAUDE.md 示例
# 路径：~/.claude/CLAUDE.md
# 跨项目生效，个人偏好

- 所有对话使用中文，commit message 用英文
- 改动超过 3 个文件时，先列出修改计划让我确认
- 遇到不确定的 3GPP 条款，标注 "VERIFY:" 让我人工确认
- 默认使用 VSCode + clangd 作为开发环境
- 代码搜索优先用 Grep，不要用 find + xargs
- 调试 C/C++ 问题时优先查看 Coverity 报告
